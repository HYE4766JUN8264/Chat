plugins {
    id 'java' // Java 플러그인 적용
}

group 'com.example' // 그룹 ID (필요시 변경 가능)
version '1.0.0' // 버전 정보

repositories {
    mavenCentral() // 중앙 저장소에서 의존성 가져오기
}

dependencies {
    // Gradle 의존성 추가: JSON 처리를 위해 Gson 또는 Jackson 사용.
    implementation 'com.google.code.gson:gson:2.8.8'
}
